name: zentheme
description: Deploy a zensical website with BrokenSource commons

runs:
  using: composite
  steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uvx --with git+https://github.com/BrokenSource/Zentheme/ pages-make
      shell: bash
